services:
  app:
    build: .
    image: pack-calculator:latest
    container_name: pack-calculator
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      DB_PATH: "/data/app.db"
    volumes:
      - pack-data:/data
    restart: unless-stopped

volumes:
  pack-data:
